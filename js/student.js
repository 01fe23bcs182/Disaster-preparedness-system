const imSafeBtn = document.getElementById('im-safe');

imSafeBtn.addEventListener('click', () => {
  alert("Your 'I'm Safe' status has been recorded (simulation mode).");
});

// You can simulate live drill updates if needed
